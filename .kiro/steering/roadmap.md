# 🗺️ Roadmap: Інтеграція "Vector Logic" в екосистему Дія (Yana Engine)

**Концепція:** Перехід від "Pixel-Based Design" (Figma/PNG/CSS) до "Formula-Based Design" (Mathematical Vector Logic).
**Мета:** Створення єдиного джерела правди (Single Source of Truth), де інтерфейс описується математично, а рендериться нативно на будь-якій платформі (Web, iOS, Android, Print) з мінімальними витратами ресурсів.

-----

## 🏛️ Етап 1: Фундамент та Валідація (Q1 - The Foundation)

*Мета: Довести, що математика краща за DOM на рівні ядра.*

Базується на твоєму репо `UI-Matrix-Benchmark`.

### 1.1. Розробка специфікації `D-Vec` (Diia Vector Format)

Ми маємо замінити хаотичний HTML на строгий JSON-протокол. Це "ДНК" інтерфейсу.

  * **Дія:** Створити типізацію у `yana-diia-v3/lib/types/dvec.ts`.
  * **Структура `D-Vec`:**
    ```typescript
    type DiiaVectorComponent = {
      id: string;
      type: "card" | "button" | "input";
      // Геометрія не в пікселях, а в відносних формулах
      geometry: {
        width: "screen.width - 32", // Math expression
        height: "content.height + 24",
        radius: "theme.radius.md"
      };
      // Стилі прив'язані до дизайн-системи Дії
      style: {
        fill: "theme.colors.bgGeneric", // #F3F5F8
        stroke: "theme.colors.borderDefault"
      };
      // Accessibility (Критично для Дії!)
      a11y: {
        role: "button",
        label: "Підписати документ"
      }
    }
    ```

### 1.2. Адаптація шрифту `e-Ukraine`

Дія використовує кастомний шрифт. Векторний рушій повинен вміти рендерити текст як криві (paths) для ідеальної точності, або використовувати нативний шрифт пристрою для швидкості.

  * **Завдання:** Інтегрувати метрики шрифту `e-Ukraine` у формули розрахунку ширини тексту в `VectorGenerator`.

### 1.3. Accessibility Layer (Інклюзивність)

Офіційна вимога Дії — відповідність стандарту **WCAG 2.1**.

  * **Рішення:** Твій генератор SVG повинен автоматично додавати теги `<title>`, `<desc>` та атрибути `aria-*` всередину SVG. Вектор стає "читабельним" для скрінрідерів.

-----

## 🌉 Етап 2: Гібридний Міст (Q2 - The Bridge)

*Мета: Зробити технологію зручною для дизайнерів (Figma) та AI-агента (Yana).*

### 2.1. Yana як "Транслятор Дизайну"

Інтеграція в твій основний проєкт `yana-diia-v3`.

  * **Задача:** Навчити Яну (твого AI-агента) приймати на вхід опис "Хочу картку єДокумента" і видавати чистий `D-Vec` JSON, а не React-код.
  * **Реалізація:** Оновлення `lib/llm/pipeline/flow-generator.ts`. Яна стає архітектором формул.

### 2.2. Figma-to-Vector Converter (Plugin)

Дизайнери Дії не будуть писати код. Їм треба дати інструмент.

  * **Інструмент:** Плагін для Figma, який бере виділений фрейм і конвертує його в твою формулу.
  * **Логіка:** Замість "експортувати як PNG", плагін аналізує: "Ага, це прямокутник з заокругленням 16px, це відповідає `DiiaCard`".
  * **Результат:** Дизайнер малює -> Яна оптимізує в формулу -> Розробник отримує готовий код.

-----

## 🚀 Етап 3: Мультиплатформна Експансія (Q3 - Native Scale)

*Мета: Генерація нативного коду з однієї формули.*

Це ключовий етап для екосистеми "Держава у смартфоні".

### 3.1. Генератори для мобільних платформ

Твій `vector-renderer.js` зараз робить SVG. Треба додати адаптери:

  * **iOS (SwiftUI):** Формула `Rect(w, h)` перетворюється на `Path { p.addRect(...) }`.
  * **Android (Jetpack Compose):** Перетворюється на `Canvas(modifier) { drawRect(...) }`.
  * **PDF (Docs):** Генерація офіційних бланків для друку з ідеальною векторною якістю (300 DPI).

### 3.2. "Infinite Responsive" (Адаптивність)

Реалізація логіки, де один і той самий `D-Vec` файл коректно відображається на:

  * Smart Watch (спрощена геометрія).
  * Smartphone (стандарт).
  * Desktop/Tablet (розширена геометрія).
  * **Логіка:** Формули використовують змінні середовища (`env.viewportType`), автоматично перебудовуючи макет.

-----

## 🏛️ Етап 4: Стандартизація (Q4 - The Standard)

*Мета: Впровадження як Open Source стандарту для GovTech.*

### 4.1. Створення "Diia Vector UI Kit"

Публікація NPM-пакету `@diia/vector-ui`.

  * Він містить базові компоненти (кнопки, інпути, хедери), описані математично.
  * Будь-який підрядник, що робить сайт для держструктури, може використати цей пакет і отримати гарантовано швидкий та правильний дизайн.

### 4.2. Інтеграція з Low-Code (Lego Builder)

Повна інтеграція в твій модуль `app/(builder)/lego/page.tsx`.

  * Користувач перетягує блоки.
  * Під капотом формується математична модель.
  * На виході — найшвидший у світі державний портал.

-----

## 📊 Порівняльна таблиця (Для презентації)

| Характеристика | Поточний підхід (Figma -> React) | **Yana Vector Protocol** | Ефект для Дії |
| :--- | :--- | :--- | :--- |
| **Джерело правди** | Макети Figma (візуальне) | Математична формула (логічне) | Виключення помилок верстки |
| **Розмір сторінки** | ~500 KB (HTML/CSS/JS) | ~5-10 KB (JSON/Math) | **Прискорення в 50 разів** (2G мережі) |
| **Адаптивність** | Media Queries (складно) | Recalculation (автоматично) | Ідеально на будь-якому екрані |
| **Мобільна розробка**| Окрема верстка | Авто-генерація коду | Скорочення часу розробки на 40% |
| **ШІ-генерація** | Дорого (багато токенів) | Дешево (стислий JSON) | **Економія бюджету на AI** |

### 🛠️ Технічний стек реалізації:

1.  **Core:** TypeScript (для суворої типізації формул).
2.  **Render Engine:** Vanilla JS (Web), Swift (iOS), Kotlin (Android).
3.  **Bridge:** Figma Plugin API + Custom AI Agents (Yana).
4.  **Distribution:** PWA + NPM Packages.

Цей роадмап показує, що ти не просто "граєшся з кодом", а пропонуєш зрілу, індустріальну архітектуру, яка вирішує конкретні болі держави: **швидкість, гроші, доступність**.
